# Minimal runtime dependencies for the RunPod Audio QA worker.
# PyTorch is installed separately with CUDA support in the Dockerfile.

# Core audio / ML
demucs==4.0.1
audio-separator==0.47.0
scipy==1.14.0
numpy==2.0.2
soundfile==0.12.1

# HTTP / parsing
requests==2.32.3
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1
pyyaml==6.0.1

# YouTube download fallback for the render pipeline (imported at module level
# by src/karaoke_generator.py)
yt-dlp==2024.12.13

# Web server (Load Balancer)
fastapi==0.112.0
uvicorn==0.30.5

# Note: runpod SDK is intentionally omitted from the worker image.
# The Load Balancer worker is a plain FastAPI/uvicorn app.
